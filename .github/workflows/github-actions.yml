name: Check pull request source branch
on:
  pull_request_target:
    types:
      - opened
      - reopened
      - synchronize
      - edited
jobs:
  check-branches:
    runs-on: ubuntu-latest
    steps:
      - name: Check branches
        run: |
          if [ ${{ github.head_ref }} != "testfrom" ] && [ ${{ github.base_ref }} == "testto" ]; then
            echo "Merge requests to testto branch are only allowed from testfrom branch."
            exit 1
          fi
