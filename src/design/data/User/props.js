const props = {
  username: {
    description: "Displays the username that is currently logged in",
  },
  client: {
    description: "Displays the client that the current user is representing",
  },
};

export { props };
